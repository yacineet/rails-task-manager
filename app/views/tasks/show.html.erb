<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<br>
  <%= link_to "Modifier", edit_task_path(@task) %>
<br>
  <%= link_to "Supprimer", task_path(@task),
      data: {turbo_method: "delete", turbo_confirm: "Are you sure?"}
  %>
<br>
<%= link_to "Retour à la liste", tasks_path%>
